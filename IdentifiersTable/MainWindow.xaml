<Window x:Class="IdentifiersTable.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IdentifiersTable"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding Path=AddTextBox}"/>
        <Button Grid.Row="0" Grid.Column="1" Command="{Binding Path=AddCommand}"/>

        <ListView Grid.Row="0" Grid.Column="2" ItemsSource="{Binding Path=Identifiers}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=simpleIndex}">Индекс</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=value}">Идентификатор</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=indexInHashTable}">Индекс в хеш таблице</GridViewColumn>
                </GridView>
            </ListView.View>
            
        </ListView>
    </Grid>
</Window>
